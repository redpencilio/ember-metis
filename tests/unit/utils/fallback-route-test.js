import fallbackRoute from 'dummy/utils/fallback-route';
import { module, test } from 'qunit';

module('Unit | Utility | fallbackRoute', function() {

  // TODO: Replace this with your real tests.
  test('it works', function(assert) {
    let result = fallbackRoute();
    assert.ok(result);
  });
});
